import 'package:flutter/material.dart';

class DevHanif extends StatelessWidget {
  const DevHanif({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Text('Developer Hanif Branch'),
          ],
        ),
      ),
    );
  }
}
