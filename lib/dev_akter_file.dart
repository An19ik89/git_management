import 'package:flutter/material.dart';

class DevAkter extends StatelessWidget {
  const DevAkter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Text('Developer Akter Branch'),
          ],
        ),
      ),
    );
  }
}